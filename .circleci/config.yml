version: 2
jobs:
  build:
    docker:
      - image: circleci/node:4.8.2
    steps:
      - checkout
      - run: echo "A first hello"
      - run:
          name: install-npm
          command: npm install
      - run:
          name: test
          command: npm test
      - run:
          name: test
          command: npm build